{{- $p := index .themes .theme -}}
[user]
    name = {{ .git_name }}
    email = {{ .git_email }}

[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only --line-numbers

[delta]
    side-by-side = true
    line-numbers = true
    navigate = true
    syntax-theme = base16
    minus-style = bold "{{ $p.base08 }}"
    plus-style = bold "{{ $p.base0B }}"
    file-style = bold "{{ $p.base0E }}"
    hunk-header-style = omit
    line-numbers-minus-style = "{{ $p.base08 }}"
    line-numbers-plus-style = "{{ $p.base0B }}"
    line-numbers-zero-style = "{{ $p.base03 }}"

[credential]
{{- if eq .chezmoi.os "darwin" }}
    helper = osxkeychain
{{- else }}
    helper = cache --timeout=3600
{{- end }}
