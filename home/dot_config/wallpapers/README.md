# Wallpapers

Desktop backgrounds stored in the dotfiles and synced to `~/.config/wallpapers/`
by chezmoi. There is **no auto-apply** — pick one manually in your OS settings.

Drop image files (`*.png`, `*.jpg`, `*.jpeg`, `*.webp`) directly in this folder.
They are treated as binary by `.gitattributes` so diffs stay clean.
