# Machine-local secrets/overrides (git-ignored + chezmoi-ignored). Optional.
# Put per-machine secrets/exports here; this file is never committed.
[[ -f ~/.config/zsh/local.zsh ]] && source ~/.config/zsh/local.zsh
