# macOS
.DS_Store

# Ansible per-machine vars (never committed)
ansible/local.yml

# Ansible local runtime artifacts (galaxy collections/roles cache)
.ansible/

# Brainstorming session artifacts (local-only)
.superpowers/

# Claude Code project dir (local-only)
.claude/
